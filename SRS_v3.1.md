# 軟體需求規格說明書 (SRS) v3.1: 專業級 ETF 即時監控系統
**版本日期**： 2026-03-24
**最後校正日期**： 2026-07-29（依實際程式碼行為校正，見各節註記；同日移除歷史資料庫功能，見 1.4 節）
**開發環境**： Python 3.13 / 3.14+ (Pandas 3.0+)
**目標終端**： Windows Terminal (適配 Solarized Light 主題)，非阻塞輸入依賴 `msvcrt`，僅支援 Windows。

## 1. 核心功能需求 (Functional Requirements)

### 1.1 資料抓取與整合 (Data Integration)
- **資料來源**：證交所整合型端點 `https://mis.twse.com.tw/stock/data/all_etf.txt`，單一請求即回傳已包含上市 (TWSE) 與上櫃 (TPEx) 的 ETF 資料。
  - 舊版規劃的 `mis.twse.com.tw/stock/api/getEtfNav.jsp?ex=tse` 與 `mis.tpex.org.tw/stock/api/getEtfNav.jsp?ex=otc` 兩個端點已失效（實測回應 404 / 302），現已停用。
- **自動更新**：系統每 **15 秒** 自動重新請求 API（無論上次成功或失敗，皆固定間隔 15 秒後重試）。
- **整合邏輯**：回應 JSON 的 `a1` 鍵下包含多個區段，各區段 `msgArray` 攤平合併為單一清單，再依「代號」升冪排序。
- **欄位映射**：API 回傳鍵值 `a`=代碼、`b`=名稱、`e`=市價、`f`=淨值、`g`=折溢價(%)、`c`=已發行單位、`d`=單位差異；`c`、`d` 兩欄實測單位為「十股」，程式會乘以 10 換算為「股」以對齊官網顯示。

### 1.2 監控清單管理 (Watchlist Management)
- **持久化清單**：監控清單存於 `watchlist.json`；首次啟動若檔案不存在，載入預設清單 `00713, 0050, 0052, 006208, 00891, 00727B, 00918, 00984D` 並建立該檔案。此後的新增/移除皆會即時寫回 `watchlist.json`，下次啟動自動沿用，不再是固定清單。
- **動態新增/移除**：支援非阻塞式輸入，指令格式：
  - 直接輸入代號（如 `0056`）或 `+0056` → 新增至監控清單
  - `-0056` → 從監控清單移除

### 1.3 資訊顯示與 UI (UI/Display)
- **欄位要求**：項次、代碼、名稱、折溢價 (%)、市價、預估淨值、已發行單位、單位差異（項次欄於表格左右兩側各顯示一次，方便寬表格對照）。
- **刷新機制**：使用 `Rich` `Live(screen=True)` 進入全螢幕替代畫面並以每秒 10 次頻率局部重繪，非逐次執行 Clear Screen；效果上不會有文字堆疊。
- **分頁瀏覽**：當清單超過單頁可顯示筆數時，支援 ↑ / ↓ 及 PageUp / PageDown 切換頁碼。

### 1.4 歷史資料（已移除）
- 曾實作 SQLite (`etf_data.db`) 歷史紀錄、異常觸發存檔、30 分鐘定時快照，以及 `save` / `clean` / `export` 指令，於 2026-07-29 移除（`database.py` 已刪除）。
- **移除原因**：本程式非全天候執行，僅在使用者開啟期間才會有資料，樣本本身就有時段偏差；加上原本「折溢價絕對值 > 0.5% 即每 15 秒存一次」的存檔邏輯，會讓異常時段的資料量遠大於平常時段，使任何事後平均/統計都會被嚴重扭曲而失去參考價值。與其維護一個看似精確卻不可靠的統計，決定僅保留即時顯示。
- 若未來需求變成「程式全天候執行」或有明確的分析目的，再評估是否重新導入資料庫，屆時建議一併記錄每筆資料的存檔原因（異常 / 定時 / 手動），並以「事件」而非逐筆時間序列的方式統計，以避免上述失真問題再次發生。

## 2. 非功能需求 (Non-functional Requirements)

### 2.1 效能 (Performance)
- **響應時間**：單次 API 請求、資料清洗至畫面渲染完成，總延遲目標低於 **2 秒**。
- **資源佔用**：善用 Pandas 的 Copy-on-Write 模式（`pd.options.mode.copy_on_write = True`），確保在高頻刷新下 CPU 佔用率低；若執行環境為 Python 3.14，額外受益於其 JIT 效能改善。

### 2.2 穩定性與異常處理 (Stability & Exception Handling)
- **容錯機制**：遇到網路斷線或證交所 API 暫時無回應時，系統不得崩潰。實作上 `api_client.py` 已在內部攔截 `httpx.ConnectError` 及其他例外，記錄至 `execution.log` 後回傳空清單，不會讓例外往上拋。
- **狀態回饋**：狀態列會顯示 `更新失敗: <例外類型>` 的簡短訊息；具體錯誤內容（如 HTTP 狀態碼、JSON 解析錯誤細節）記錄在 `execution.log`，不直接顯示於畫面。重試間隔固定為 **15 秒**（與正常更新週期相同，並非獨立的 5 秒重試）。
- **已知落差**：若 API 回應成功但內容為空（例如非交易時段），目前不會觸發例外，狀態列仍會顯示「數據更新成功」；這點與「顯示具體錯誤碼」的原始設計意圖有落差，待後續調整。

### 2.3 易讀性與視覺優化 (Readability & Visuals)
- **排版對齊**：使用 `Rich` `Table` 元件（`main.py` 的 `generate_table()`），確保欄位於終端機中對齊。
- **配色方案**：
  - 完美適配 **Solarized Light** 主題背景。
  - **溢價 > 0.5%**：數值顯**粗體紅色** (#DC322F)。
  - **折價 < -0.5%**：數值顯**粗體綠色** (#859900)。
  - **已發行單位差異**：正值顯紅色、負值顯綠色，其餘顯示中性色 (#586E75)，用於觀察申購/贖回動向。
- **視覺友善**：針對高近視需求使用者，折溢價數值加粗顯示。

## 3. 並行處理 (Concurrency)
- **非阻塞輸入**：使用 `asyncio` 搭配 Windows `msvcrt`（`kbhit()` / `getch()`），以每 10 毫秒輪詢一次的方式逐位元組讀取鍵盤輸入並組裝 UTF-8 字元，**未使用** Python 內建 `input()`。背景的 15 秒數據更新與畫面重繪在獨立的 asyncio 工作中持續運作，不受輸入影響。
- **非 Windows 環境**：`msvcrt` 不存在時，輸入功能會停用（僅記錄一次警告），但資料更新與畫面顯示仍可運作。

## 4. 技術備註 (For Antigravity)
- 使用 `match-case` 處理 API 回應的 HTTP 狀態碼（`api_client.py` 中 `match response.status_code`）。
- 利用 `Rich` 庫處理 ANSI 顏色渲染，以達成 Solarized Light 的配色要求。
- `requirements.txt` 中列有 `aioconsole`，但目前程式碼未實際引用，非阻塞輸入完全由 `msvcrt` 實作；如未來要跨平台支援，才需要真正導入 `aioconsole` 或等效方案。
